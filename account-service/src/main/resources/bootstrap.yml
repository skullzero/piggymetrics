spring:
  application:
    name: account-service
  cloud: 
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: admin
      username: user
      name: account-service
 
server:
  port: 38080
